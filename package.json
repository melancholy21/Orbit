{
  "name": "orbit-root",
  "version": "1.0.0",
  "description": "Unified package manager config for Orbit on Render",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --production=false",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}
